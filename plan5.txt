We also need to update `ResourcesViewModelTest.kt`:

```kotlin
<<<<<<< SEARCH
    @Test
    fun `observeOpenedResourceIds updates openedResourceIds state flow`() = runTest {
        val mockIds = setOf("res1", "res2")
        every { resourcesRepository.observeOpenedResourceIds("user123") } returns flowOf(mockIds)

        viewModel.observeOpenedResourceIds("user123")
        advanceUntilIdle()

        assertEquals(setOf("res1", "res2"), viewModel.openedResourceIds.value)
    }
=======
    @Test
    fun `getOpenedResourceIds returns flow from repository`() = runTest {
        val mockIds = setOf("res1", "res2")
        every { resourcesRepository.observeOpenedResourceIds("user123") } returns flowOf(mockIds)

        val flow = viewModel.getOpenedResourceIds("user123")
        advanceUntilIdle()

        assertEquals(setOf("res1", "res2"), flow.first())
    }
>>>>>>> REPLACE
```
Wait, I will use `replace_with_git_merge_diff` on `ResourcesViewModelTest.kt` too.
