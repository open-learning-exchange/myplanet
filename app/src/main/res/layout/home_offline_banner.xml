<?xml version="1.0" encoding="utf-8"?>
<!-- home_offline_banner.xml — shown only when offline -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/offline_banner_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="12dp"
    android:background="@drawable/bg_offline_banner"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <FrameLayout
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:background="@drawable/bg_offline_icon">

        <ImageView
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_cloud_off"
            app:tint="@color/offline_ink"
            android:contentDescription="@null" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/offline_headline"
            android:textColor="@color/dash_ink"
            android:textSize="13sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/txt_queue_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/offline_queue_3"
            android:textColor="@color/dash_ink_soft"
            android:textSize="11sp"
            android:layout_marginTop="1dp" />
    </LinearLayout>

    <TextView
        android:id="@+id/btn_retry_sync"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/retry"
        android:textColor="@color/dash_ink"
        android:textStyle="bold"
        android:textSize="12sp"
        android:paddingStart="10dp"
        android:paddingEnd="4dp"
        android:clickable="true"
        android:focusable="true" />
</LinearLayout>
