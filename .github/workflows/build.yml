name: myPlanet build

on:
  push:
    branches:
      - '*'
      - '!master'
      - 'claude/**'
      - 'codex/**'
      - 'dependabot/**'
      - 'jules/**'
      - '*-codex/**'

jobs:
  build:
    name: myPlanet build
    runs-on: ubuntu-24.04
    steps:
      - name: checkout repository code
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: setup gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          gradle-version: wrapper
          cache-disabled: false
          cache-read-only: false
          cache-write-only: false

      - name: build debug as test
        run: |
          ./gradlew assembleDefaultDebug assembleLiteDebug --warning-mode all --stacktrace --parallel --max-workers=4
