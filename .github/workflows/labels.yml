name: myPlanet labels

on:
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:
    inputs:
      pr:
        description: 'pull request number to (re)label'
        required: true
        type: string
      dry_run:
        description: 'report what would happen, change nothing'
        required: false
        default: false
        type: boolean

permissions:
  pull-requests: write

concurrency:
  group: labels-${{ inputs.pr || github.event.pull_request.number }}
  cancel-in-progress: true

env:
  GRADLE_FILE: app/build.gradle
  EXCLUDE_PATHS: app/src/main/res/values-*/strings.xml

jobs:
  size:
    name: myPlanet labels
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: checkout repository code
        uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: label the pull request by size
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR: ${{ inputs.pr || github.event.pull_request.number }}
          DRY_RUN: ${{ inputs.dry_run || false }}
        run: |
          chmod +x .github/scripts/labels.sh
          .github/scripts/labels.sh
