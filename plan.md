1. **Remove unused method in BaseRecyclerFragment:** The `filterCourseByTag` method in `BaseRecyclerFragment` has 0 usages because it was orphaned. I will remove it entirely.
2. **Remove orphaned import in BaseRecyclerFragment:** I will remove the `import org.ole.planet.myplanet.model.RealmMyCourse.Companion.getAllCourses` import since it is no longer used.
3. **Address `normalizeText` duplication:** The `normalizeText` method exists in both `BaseRecyclerFragment` and `CoursesRepositoryImpl`. I will move the implementation out of `CoursesRepositoryImpl` into an existing `StringUtils` or equivalent, or just delegate from `CoursesRepositoryImpl` if `BaseRecyclerFragment` has it. Actually, wait, `CoursesRepositoryImpl` doesn't have access to `BaseRecyclerFragment`. I will create a `StringUtils` object if it doesn't exist, or find an existing utility.
