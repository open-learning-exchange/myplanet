# Phase 133 — harvest the 39 commits master had drifted by

Lane C of a three-lane parallel round. Branch `claude/harvest-master-39-v7c3`,
branched from `claude/kotlin-flutter-dart-migration-d3gmrd`.

Two tasks:

1. Harvest the 39-commit upstream backlog — the largest this port has had —
   and decide, commit by commit, which changes the port must follow.
2. Add Phase 113's `exam`/`survey` table routing to the *Faithful quirks /
   deliberate deviations* list in `docs/kotlin-to-flutter-migration.md`, which
   has never carried it despite Phase 128 resting on it.

Status: in progress. Triage running.
