# Phase 122 — the four columns Phase 119 deferred

In progress. Scope: `rating.user`, `rating.createdOn`, `team_tasks.sync`,
`team_tasks.link` at `schemaVersion` 46, plus the two pre-existing
`MyLibraryMapper` defects Phase 119 reported (`'tag'` vs `'tags'`, and
`privateFor` read as a string).
