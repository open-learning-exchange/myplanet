# The Agent Spellbook — Summoning Other AIs on PRs

> Field-verified live on [PR #15436](https://github.com/open-learning-exchange/myplanet/pull/15436) (2026-08-07/08), including each agent fact-checking its own row. Core distinction: **Reviewers** speak; **Doers** act — an unleashed Doer mention defaults to commits on your branch.
>
> This page is the reference: who answers to what, how fast, and with which side effects. The behavioral rules — the **Laws of Summoning** — live in `CLAUDE.md` → "The Agent Spellbook", so the agents that ingest that file carry them as standing instructions.

## The Grid

| Agent (identity) | Type | Summon | Ack | Pushes to your branch? | Quirks & limits |
|---|---|---|---|---|---|
| **CodeRabbit** (`coderabbitai[bot]`) | Reviewer | auto on push + `@coderabbitai review` / `full review` / `resolve` / `approve` / `fix ci [commit]` / `autofix [stacked pr]` / `resolve merge conflict` / `generate {docstrings, unit tests, sequence diagram}` / `configuration` / `pause`·`resume` / `help` — `ignore` works only in the PR **description** | minutes | only opt-in (`fix ci commit`, `autofix`, generators) | auto-review skips drafts and pauses after 5 reviewed commits (config); allowance: 5 reviews/dev/hr (Pro; Pro+ 10, rolling); review fails if head moves mid-review; `approve` needs `reviews.request_changes_workflow: true`; chats in threads (Q&A, issue creation to GitHub/Jira/Linear); stores teachable per-repo learnings |
| **Codex** (`chatgpt-codex-connector[bot]`) | Reviewer (+ cloud tasks) | `@codex review` · targeted: `@codex review for issues in <scope>` (auto on open/ready **only if enabled** in repo Codex settings) | 👀 in seconds; review in minutes; 👍 if clean | no — `@codex fix it` / `address that feedback` starts a cloud task that may update the PR **or** deliver a sibling branch/PR | P0–P3 badges observed here (docs claim GitHub reviews flag only P0/P1); repo rules via `## Code Review Rules` in `AGENTS.md`; `codex` label, `*-codex/*` branches |
| **Copilot** (`Copilot` / `copilot-swe-agent`; Reviewers-UI reviews author as `copilot-pull-request-reviewer[bot]`) | Doer, instruction-following | `@copilot <ask>` (write-access users only) · Reviewers UI (review-only) · assign an issue | ~30 s | **default** — docs: mentions work on any PR and push to that branch; say "open a separate PR" to redirect | Reviewers-UI review is Comment-only (never approves, no auto re-review) — and produced **no review at all** on this repo when formally requested; issue assignment spawns its own `copilot/**` PR; coding agent reads `.github/copilot-instructions.md` + nearest `AGENTS.md`, with a root `CLAUDE.md`/`GEMINI.md` as the **alternative** (an `AGENTS.md` takes precedence and unbinds `CLAUDE.md`); code review reads `AGENTS.md` but not `CLAUDE.md` |
| **Devin** (`devin-ai-integration[bot]`) | Doer, instruction-following | `@devin <ask>` | session link ~10 s | yes, unless leashed ("comment only" held in test) | one session **adopts** the PR — later mentions join it (no races from Devin); ⚠️ commit identity is a configurable "commit authoring mode" — here its commits wear the requesting user's identity, so audit via PR timeline, not `git log`; Knowledge ingests `CLAUDE.md`/`AGENTS.md`; verify its claims (env may lack the Android SDK) |
| **OpenHands** (`openhands-ai[bot]`) | Doer, unleashable | `@openhands <ask>` (not `@allhands`) · `openhands` label on an issue | "I'm on it!" + session link ~10 s | yes — reads *any* mention (even "help") as "fix what's open", and **pushed even against an explicit no-push instruction** (verified) | **new session per mention** (4+ concurrent observed); double-posts under both the mentioning user's account and the bot; commits authored `openhands`; standing context via `.openhands/microagents/repo.md` (prompt-level, not a guardrail); disputed its own receipts-documented behavior in self-review — trust the timeline |
| **Jules** (`google-labs-jules[bot]`) | Issue-driven | `jules` label on an issue (reliable) / Jules app · PR feedback works only on **Jules-created PRs** via a submitted review (👀 ack per comment, then pushes fixes) — by default it acts on *every* review comment; opt-in Reactive Mode **narrows** that to explicit `@Jules` mentions. Mentions on foreign PRs (like this one) go silent — no session owns the branch | — | own `jules-*` PRs only — but there it **does** push follow-up commits in response to accepted review feedback, not just the initial branch | 20+ bot-authored PRs; reads `AGENTS.md` + per-repo memory (no `CLAUDE.md` support); configurable commit-authoring modes (Jules / co-authored / user-only) — audit via PR timeline; quotas 15/100/300 tasks/day by plan |
| **Claude Code** | Session Doer | claude.ai/code session — no mention-bot (`@claude` silent) | — | its own `claude/**` branch | can subscribe to PR events and drive to green from inside a session |
| **Dependabot** (`dependabot[bot]`) | Scheduled | daily; `@dependabot rebase` / `recreate` / `ignore …` / `unignore …` / `show … ignore conditions` on **its own PRs only** | — | own PRs | Actions + Gradle bumps (`.github/dependabot.yml`) |

## Farther shelves — summonable, not yet field-tested here

Web-researched against official docs 2026-08-08, **not yet summoned on this repo** — ack times and quirks below are vendor claims, not observation. Promote a row into the field-tested grid by casting it here and recording receipts (Law 9: bring evidence, don't write your own entry).

| Agent (identity) | Type | Summon | Ack | Pushes to your branch? | Quirks & limits |
|---|---|---|---|---|---|
| **Amazon Q Developer** (`amazon-q-developer[bot]`) | **Doer** | auto review on PR open/reopen (not later commits) · `/q review` or `/q <ask>` as a **new top-level** comment (summoner needs Write+) · issue labels `Amazon Q development agent` / `transform agent` spawn its own PR | minutes | **yes** — `/q <ask>` "creates commits directly on your pull request's source branch"; review findings arrive as committable suggestions | preview; free monthly reviewed-lines quota; custom rules in `.amazonq/rules/*.md`; ⚠️ brand migrating to Kiro (IDE EoS 2027) — the GitHub app's long-term fate is unconfirmed |
| **Cursor Bugbot** (`cursor[bot]`) | Reviewer (opt-in Doer via Autofix) | auto on PR create/update · `bugbot run` / `cursor review` as a **standalone** top-level comment (replies ignored; `verbose=true` for details) — ⚠️ on individual-plan installs it runs **only on PRs you author**; Team/Enterprise covers all contributors | "Cursor Bugbot" status check; minutes | no by default — inline comments with "Fix in Cursor" / "Fix in Web" links; **Autofix** spawns a cloud agent that can push (retry cap unsourced) | inline diff comments only, no summary/verdict; reads existing PR comments as context; rules via `.cursor/BUGBOT.md` (not `.cursor/rules/`); paid plans, usage-based; absorbing Graphite Diamond after Cursor's Graphite acquisition |
| **Greptile** (`greptile-apps[bot]`, mention `@greptileai`) | Reviewer | auto on open + new commits · `@greptileai <ask>` re-triggers, scoped asks work (`@greptileai check for memory leaks`, `review this draft`) | review in ~1–5 min | no — comments + suggestion blocks (shows as commit co-author when humans apply them); can auto-approve clean reviews if enabled | indexes the whole repo for context; learns from 👍/👎 reactions; free 50 reviews/mo; **free for OSS** |
| **Sourcery** (`sourcery-ai[bot]`, mention `@sourcery-ai`) | Reviewer | auto-review seconds after open · `@sourcery-ai review` / `summary` / `guide` / `title` / `resolve` / `dismiss` as a root comment; chat in threads | seconds | no | **free for public repos**; ignores comments from other bots (loop prevention); 👍/👎 tunes future reviews; per-PR re-review counter |
| **Qodo Merge / PR-Agent** (hosted `qodo-merge-pro[bot]`; OSS app `qodo-merge-pro-for-open-source[bot]`) | Reviewer, command-driven | `/review` / `/improve` / `/ask <q>` / `/describe` / `/help` as PR comments (also inline `/ask` on code lines); auto on open, configurable | one structured comment per command, fast | no — `/improve` suggestions applied via checkbox (a human commits); `/describe` edits PR title/description | dual nature: Apache-2.0 **PR-Agent** self-hosted with your API key, or hosted Qodo Merge (free tier ~30 PRs/mo); **free-for-OSS** Marketplace app |
| **Ellipsis** (mention `@ellipsis`; current bot login unconfirmed) | Reviewer + on-request Doer | auto comment-only review per push · `@ellipsis <ask>` in any PR/issue comment | seconds–minutes | only on explicit ask (`@ellipsis fix X and open a PR` runs a cloud agent); docs: "never commit code without your explicit permission" | pivoted to a cloud-agents platform, review is one feature; incremental re-reviews per push; no OSS free tier found |

Long tail, unverified beyond roundups: **Korbit** (`korbit-ai[bot]`, `/korbit-review`), **Devlo** (`devloai[bot]` — a mention-summoned Doer), **cubic** (`cubic-dev-ai[bot]`), **Entelligence**. **Gemini Code Assist** (`gemini-code-assist[bot]`, `/gemini review`) is grid-worthy but its consumer/free tier **shut down 2026-07-17** — enterprise Google Cloud only now. **Sweep AI** is defunct as a GitHub bot (IDE-only since its pivot). **reviewdog** is a deterministic linter bridge, not a summonable agent.

## Vendor grimoires

Official references; the grid above records observed behavior on this repo where the two differ.

- CodeRabbit: [review commands](https://docs.coderabbit.ai/reference/review-commands) · [configuration](https://docs.coderabbit.ai/reference/configuration) · [learnings](https://docs.coderabbit.ai/knowledge-base/learnings) — `@coderabbitai configuration` prints this repo's live config
- Codex: [GitHub integration](https://learn.chatgpt.com/docs/third-party/github)
- Copilot: [code review](https://docs.github.com/en/copilot/concepts/agents/code-review) · [changing existing PRs](https://docs.github.com/en/copilot/how-tos/use-copilot-agents/cloud-agent/make-changes-to-an-existing-pr)
- Devin: [GitHub integration](https://docs.devin.ai/integrations/gh) (commit authoring modes) · [Knowledge](https://docs.devin.ai/product-guides/knowledge)
- OpenHands: [GitHub cloud](https://docs.openhands.dev/openhands/usage/cloud/github-installation) · [repo microagents](https://docs.openhands.dev/modules/usage/prompting/microagents-repo)
- Jules: [running tasks](https://jules.google/docs/running-tasks/) · [usage limits](https://jules.google/docs/usage-limits/)
- Dependabot: [comment commands](https://docs.github.com/en/code-security/reference/supply-chain-security/dependabot-pull-request-comment-commands) — current list is `rebase`, `recreate`, the `ignore` family, `show … ignore conditions`; the old merge commands are gone from dotcom docs

## Cross-cutting doc facts

- **No vendor documents a hard "never push" switch** — every standing-rule mechanism (microagents, Knowledge, `AGENTS.md`) is prompt-level context, and Law 1 (scope your summons) is best-effort, not enforcement: the OpenHands row above records a prompt leash failing. The enforceable controls are GitHub-side — branch protection/rulesets and app/repository write permissions.
- **Copilot and Devin ingest `CLAUDE.md` directly** (Copilot's coding agent lists it first-party, [confirmed on this PR](https://docs.github.com/en/copilot/reference/custom-instructions-support); Devin via Knowledge) — the Laws of Summoning bind those two simply by being merged. Two per-feature caveats: Copilot **code review** reads `AGENTS.md` but not `CLAUDE.md` (it still reads `.github/copilot-instructions.md` and path instructions), and for the coding agent the nearest `AGENTS.md` **takes precedence** over a root `CLAUDE.md`. Outside github.com the support matrix narrows further (VS Code Chat reads only `AGENTS.md`; IDE code review reads no agent-instruction file) — a second reason the symlink is the only shape that binds every environment at once. Jules reads only `AGENTS.md`, which this repo doesn't have. Consequence: to bind Jules (and Copilot review) without unbinding Copilot's coding agent from these laws, **symlink `AGENTS.md` → `CLAUDE.md`** rather than writing a separate file — a divergent `AGENTS.md` would silently replace `CLAUDE.md` for Copilot.
